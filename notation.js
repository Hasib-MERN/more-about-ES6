const notation = {
    dot: 'dot notation',
    bracket: 'bracket notation'

    // we access an property two way 1. dot notation 2. bracket notation
}

console.log(notation['dot']);
console.log(notation['bracket']);